manifest_schema_version = "1.0"

[plugin]
name = "Bad_Name"
version = "0.1.0"
description = "Name contains uppercase and underscore."
triggers = ["process_writes"]

[dependencies]
database_version = ">=3.0.0"

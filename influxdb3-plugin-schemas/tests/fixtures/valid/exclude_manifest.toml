manifest_schema_version = "1.2"

[plugin]
name = "excludes"
version = "0.1.0"
description = "A plugin exercising the exclude field."
triggers = ["process_writes"]
exclude = [".git/", ".venv/", "__pycache__/", "*.pyc", "tests/**"]

[dependencies]
database_version = ">=3.0.0"

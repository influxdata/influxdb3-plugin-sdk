manifest_schema_version = "1.0"

[plugin]
name = "minimal"
version = "0.1.0"
description = "The smallest well-formed plugin."
triggers = ["process_writes"]

[dependencies]
database_version = ">=3.0.0"

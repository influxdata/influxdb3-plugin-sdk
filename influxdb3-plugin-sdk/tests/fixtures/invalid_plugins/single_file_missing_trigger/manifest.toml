manifest_schema_version = "1.0"

[plugin]
name = "missing-trigger"
version = "0.1.0"
description = "Single .py file that does not implement the declared trigger."
triggers = ["process_writes"]

[dependencies]
database_version = ">=3.0.0"

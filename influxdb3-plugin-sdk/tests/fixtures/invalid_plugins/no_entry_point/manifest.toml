manifest_schema_version = "1.0"

[plugin]
name = "no-entry-point"
version = "0.1.0"
description = "Manifest only, no Python files at all."
triggers = ["process_writes"]

[dependencies]
database_version = ">=3.0.0"

manifest_schema_version = "1.0"

[plugin]
name = "Bad_Name"
version = "1.2"
description = "Four distinct defects: bad name, bad version, bad trigger, bad URL scheme."
triggers = ["on_startup"]
homepage = "ftp://bad"

[dependencies]
database_version = ">=3.0.0"

manifest_schema_version = "1.0"

[plugin]
name = "no-init"
version = "0.1.0"
description = "Manifest is fine but __init__.py is absent."
triggers = ["process_writes"]

[dependencies]
database_version = ">=3.0.0"

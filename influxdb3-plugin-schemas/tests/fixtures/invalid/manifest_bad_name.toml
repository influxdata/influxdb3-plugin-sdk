manifest_schema_version = "1.0"

[plugin]
name = "Bad Name"
version = "0.1.0"
description = "Name contains an ASCII space, which is not allowed."
triggers = ["process_writes"]

[dependencies]
database_version = ">=3.0.0"

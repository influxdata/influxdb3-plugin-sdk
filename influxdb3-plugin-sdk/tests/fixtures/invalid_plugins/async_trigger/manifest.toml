manifest_schema_version = "1.0"

[plugin]
name = "async-trigger"
version = "0.1.0"
description = "Trigger function is `async def`; runtime invokes synchronously."
triggers = ["process_writes"]

[dependencies]
database_version = ">=3.0.0"

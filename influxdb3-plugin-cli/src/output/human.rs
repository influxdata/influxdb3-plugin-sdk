//! Human-mode renderers per command.
//!
//! Per-command rendering functions land alongside their command modules in
//! D30 (`new`), D31 (`validate`), D32 (`package`), and D33 (`yank`). This
//! file currently holds no public items; it exists so the `output` module's
//! `mod human;` declaration resolves.

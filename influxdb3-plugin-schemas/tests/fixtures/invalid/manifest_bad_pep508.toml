manifest_schema_version = "1.0"

[plugin]
name = "bad-pep508"
version = "0.1.0"
description = "Python requirement uses invalid double-operator."
triggers = ["process_writes"]

[dependencies]
database_version = ">=3.0.0"
python = ["requests>>=2.0"]

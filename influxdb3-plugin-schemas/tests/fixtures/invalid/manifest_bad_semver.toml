manifest_schema_version = "1.0"

[plugin]
name = "bad-semver"
version = "1.2"
description = "Version string is not full SemVer 2.0.0."
triggers = ["process_writes"]

[dependencies]
database_version = ">=3.0.0"
